# EAI Studio 10.0 (Web)

## Run Locally

1. Install dependencies from repo root:
   `npm install`
2. Start API and web:
   `npm run dev`

The web app expects the API at `http://localhost:3333` (set `VITE_API_BASE` to override).
